<% link_options = {} if local_assigns[:link_options].nil? %>
<%= link_to(image_tag(photo.source, :alt => photo.title), 'http://flickr.com/actionhank/', link_options.merge({:title => 'See all our photos on Flickr'})) %>